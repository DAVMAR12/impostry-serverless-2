from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class Input(BaseModel):
    prompt: str

@app.post("/run")
def run(data: Input):
    return {"output": f"Ты отправил: {data.prompt}"}
